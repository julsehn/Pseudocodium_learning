procedure lamevafuncio {
    num_usuari <- 0
    num_int <- 0
    num_int2 <- 0
    num_pre <- 0
    num_final <- 0

    write "Dige'm un nombre: "
    read num_usuari
    while num_usuari >= num_final AND num_int2 =0 {
        num_int <- num_int + 1
        num_pre <- num_int + num_pre
        if num_pre <= num_final{
            num_final <- num per
            num_int2 <- 1
        }
    }
    write "El resultat es: ", num_final
}