procedure mitjanade10 {
    suma <- 0
    contador <- 0
    for i from 1 to 10 do {
        write "Dige'm el nombre "
        read nombre
        suma <- suma + nombre
        contador <- contador + 1
    }
    mitjana <- suma / contador
    write "La mitjana dels nombres és: ", mitjana
}